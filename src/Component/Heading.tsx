type headerProps = {
    children: string
}
const Heading = (props: headerProps) => {
    return ( 
        <>
            {/* <h2>{props.children}</h2> */}
        </>
     );
}
 
export default Heading;